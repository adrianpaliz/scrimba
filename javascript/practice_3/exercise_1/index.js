// Setting the stage
const player = "Adrián"
const opponent = "Isabel"
const game = "AmazingFighter"
let points = 0
let hasWin = false

// Playing the game
points += 100
hasWon = true

// Announcing the winner
if (hasWon) {console.log(`${player} got ${points} points and won the ${game} game!"` )
} else {
	console.log(`The winner is ${opponent}! ${palyer} lost the game`)
}

